*.bak
*.bak_dark

# 防止嵌套 .git 目录被 add 为 gitlink（会导致 Pages 构建 fatal）
# 注意: 如果子目录内已有 .git/, 即使 add 也会被记录为 gitlink(160000), 不报错但 CI 会挂
# 预防: git add 前用 `find . -name ".git" -type d -not -path "./.git"` 检查
